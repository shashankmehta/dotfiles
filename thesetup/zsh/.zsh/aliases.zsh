# GIT
alias gfo="git fetch origin"
alias gpo="git push origin"
alias gr="git rebase"

# GENERAL
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."

alias v="vim"

# TMUX + CLAUDE
alias tc="bash ~/dev/tmux-claude.sh"
